# A GraphQL Angular web app

## Getting started

If you plan to edit `*.graphql` files, you have to launch `dart tool/watch.dart`
to enable the code generator that converts `*.graphql` files into dart.

Then simply run `pub serve` and enjoy!